import LayoutAdmin from "@/layouts";
import type { NextPage } from "next";

const Home: NextPage = () => {
  return (
    <LayoutAdmin>
      <div className="text-red-900">Hello sỹ bình</div>
    </LayoutAdmin>
  );
};

export default Home;
